name: Blog RSS V2
on: push

jobs:
  fetch:
    runs-on: macos-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v2
        with:
          ref: gh-pages

      - id: 'update_worksheet_1'
        uses: jroehl/gsheet.action@release # you can specify '@release' to always have the latest changes
        with:
          spreadsheetId: ${{ secrets.GSHEET_ID }}
          commands: | # list of commands, specified as a valid JSON string
            [
              { "command": "getData",
                "args": {
                  "range": "Blog!A1:F1"
                }
              }
            ]
        env:
          GSHEET_CLIENT_EMAIL: ${{ secrets.GSHEET_CLIENT_EMAIL }}
          GSHEET_PRIVATE_KEY: ${{ secrets.GSHEET_PRIVATE_KEY }}

      - id: 'update_worksheet_2'
        uses: jroehl/gsheet.action@release # you can specify '@release' to always have the latest changes
        with:
          spreadsheetId: ${{ secrets.GSHEET_ID }}
          commands: | # list of commands, specified as a valid JSON string
            [
              { "command": "getData",
                "args": {
                  "range": "Blog!A2:F2"
                }
              }
            ]
        env:
          GSHEET_CLIENT_EMAIL: ${{ secrets.GSHEET_CLIENT_EMAIL }}
          GSHEET_PRIVATE_KEY: ${{ secrets.GSHEET_PRIVATE_KEY }}

      - id: 'update_worksheet_3'
        uses: jroehl/gsheet.action@release # you can specify '@release' to always have the latest changes
        with:
          spreadsheetId: ${{ secrets.GSHEET_ID }}
          commands: | # list of commands, specified as a valid JSON string
            [
              { "command": "getData",
                "args": {
                  "range": "Blog!A3:F3"
                }
              }
            ]
        env:
          GSHEET_CLIENT_EMAIL: ${{ secrets.GSHEET_CLIENT_EMAIL }}
          GSHEET_PRIVATE_KEY: ${{ secrets.GSHEET_PRIVATE_KEY }}

      - id: 'update_worksheet_4'
        uses: jroehl/gsheet.action@release # you can specify '@release' to always have the latest changes
        with:
          spreadsheetId: ${{ secrets.GSHEET_ID }}
          commands: | # list of commands, specified as a valid JSON string
            [
              { "command": "getData",
                "args": {
                  "range": "Blog!A4:F4"
                }
              }
            ]
        env:
          GSHEET_CLIENT_EMAIL: ${{ secrets.GSHEET_CLIENT_EMAIL }}
          GSHEET_PRIVATE_KEY: ${{ secrets.GSHEET_PRIVATE_KEY }}

      - id: 'update_worksheet_5'
        uses: jroehl/gsheet.action@release # you can specify '@release' to always have the latest changes
        with:
          spreadsheetId: ${{ secrets.GSHEET_ID }}
          commands: | # list of commands, specified as a valid JSON string
            [
              { "command": "getData",
                "args": {
                  "range": "Blog!A5:F5"
                }
              }
            ]
        env:
          GSHEET_CLIENT_EMAIL: ${{ secrets.GSHEET_CLIENT_EMAIL }}
          GSHEET_PRIVATE_KEY: ${{ secrets.GSHEET_PRIVATE_KEY }}

      - id: 'update_worksheet_6'
        uses: jroehl/gsheet.action@release # you can specify '@release' to always have the latest changes
        with:
          spreadsheetId: ${{ secrets.GSHEET_ID }}
          commands: | # list of commands, specified as a valid JSON string
            [
              { "command": "getData",
                "args": {
                  "range": "Blog!A6:F6"
                }
              }
            ]
        env:
          GSHEET_CLIENT_EMAIL: ${{ secrets.GSHEET_CLIENT_EMAIL }}
          GSHEET_PRIVATE_KEY: ${{ secrets.GSHEET_PRIVATE_KEY }}

      - id: 'update_worksheet_7'
        uses: jroehl/gsheet.action@release # you can specify '@release' to always have the latest changes
        with:
          spreadsheetId: ${{ secrets.GSHEET_ID }}
          commands: | # list of commands, specified as a valid JSON string
            [
              { "command": "getData",
                "args": {
                  "range": "Blog!A7:F7"
                }
              }
            ]
        env:
          GSHEET_CLIENT_EMAIL: ${{ secrets.GSHEET_CLIENT_EMAIL }}
          GSHEET_PRIVATE_KEY: ${{ secrets.GSHEET_PRIVATE_KEY }}

      - id: 'update_worksheet_8'
        uses: jroehl/gsheet.action@release # you can specify '@release' to always have the latest changes
        with:
          spreadsheetId: ${{ secrets.GSHEET_ID }}
          commands: | # list of commands, specified as a valid JSON string
            [
              { "command": "getData",
                "args": {
                  "range": "Blog!A8:F8"
                }
              }
            ]
        env:
          GSHEET_CLIENT_EMAIL: ${{ secrets.GSHEET_CLIENT_EMAIL }}
          GSHEET_PRIVATE_KEY: ${{ secrets.GSHEET_PRIVATE_KEY }}

      - id: 'update_worksheet_9'
        uses: jroehl/gsheet.action@release # you can specify '@release' to always have the latest changes
        with:
          spreadsheetId: ${{ secrets.GSHEET_ID }}
          commands: | # list of commands, specified as a valid JSON string
            [
              { "command": "getData",
                "args": {
                  "range": "Blog!A9:F9"
                }
              }
            ]
        env:
          GSHEET_CLIENT_EMAIL: ${{ secrets.GSHEET_CLIENT_EMAIL }}
          GSHEET_PRIVATE_KEY: ${{ secrets.GSHEET_PRIVATE_KEY }}

      - id: 'update_worksheet_10'
        uses: jroehl/gsheet.action@release # you can specify '@release' to always have the latest changes
        with:
          spreadsheetId: ${{ secrets.GSHEET_ID }}
          commands: | # list of commands, specified as a valid JSON string
            [
              { "command": "getData",
                "args": {
                  "range": "Blog!A10:F10"
                }
              }
            ]
        env:
          GSHEET_CLIENT_EMAIL: ${{ secrets.GSHEET_CLIENT_EMAIL }}
          GSHEET_PRIVATE_KEY: ${{ secrets.GSHEET_PRIVATE_KEY }}

      - id: 'update_worksheet_11'
        uses: jroehl/gsheet.action@release # you can specify '@release' to always have the latest changes
        with:
          spreadsheetId: ${{ secrets.GSHEET_ID }}
          commands: | # list of commands, specified as a valid JSON string
            [
              { "command": "getData",
                "args": {
                  "range": "Blog!A11:F11"
                }
              }
            ]
        env:
          GSHEET_CLIENT_EMAIL: ${{ secrets.GSHEET_CLIENT_EMAIL }}
          GSHEET_PRIVATE_KEY: ${{ secrets.GSHEET_PRIVATE_KEY }}

      - id: 'update_worksheet_12'
        uses: jroehl/gsheet.action@release # you can specify '@release' to always have the latest changes
        with:
          spreadsheetId: ${{ secrets.GSHEET_ID }}
          commands: | # list of commands, specified as a valid JSON string
            [
              { "command": "getData",
                "args": {
                  "range": "Blog!A12:F12"
                }
              }
            ]
        env:
          GSHEET_CLIENT_EMAIL: ${{ secrets.GSHEET_CLIENT_EMAIL }}
          GSHEET_PRIVATE_KEY: ${{ secrets.GSHEET_PRIVATE_KEY }}

      - id: 'update_worksheet_13'
        uses: jroehl/gsheet.action@release # you can specify '@release' to always have the latest changes
        with:
          spreadsheetId: ${{ secrets.GSHEET_ID }}
          commands: | # list of commands, specified as a valid JSON string
            [
              { "command": "getData",
                "args": {
                  "range": "Blog!A13:F13"
                }
              }
            ]
        env:
          GSHEET_CLIENT_EMAIL: ${{ secrets.GSHEET_CLIENT_EMAIL }}
          GSHEET_PRIVATE_KEY: ${{ secrets.GSHEET_PRIVATE_KEY }}

      - id: 'update_worksheet_14'
        uses: jroehl/gsheet.action@release # you can specify '@release' to always have the latest changes
        with:
          spreadsheetId: ${{ secrets.GSHEET_ID }}
          commands: | # list of commands, specified as a valid JSON string
            [
              { "command": "getData",
                "args": {
                  "range": "Blog!A14:F14"
                }
              }
            ]
        env:
          GSHEET_CLIENT_EMAIL: ${{ secrets.GSHEET_CLIENT_EMAIL }}
          GSHEET_PRIVATE_KEY: ${{ secrets.GSHEET_PRIVATE_KEY }}

      - id: 'update_worksheet_15'
        uses: jroehl/gsheet.action@release # you can specify '@release' to always have the latest changes
        with:
          spreadsheetId: ${{ secrets.GSHEET_ID }}
          commands: | # list of commands, specified as a valid JSON string
            [
              { "command": "getData",
                "args": {
                  "range": "Blog!A15:F15"
                }
              }
            ]
        env:
          GSHEET_CLIENT_EMAIL: ${{ secrets.GSHEET_CLIENT_EMAIL }}
          GSHEET_PRIVATE_KEY: ${{ secrets.GSHEET_PRIVATE_KEY }}

      - id: 'update_worksheet_16'
        uses: jroehl/gsheet.action@release # you can specify '@release' to always have the latest changes
        with:
          spreadsheetId: ${{ secrets.GSHEET_ID }}
          commands: | # list of commands, specified as a valid JSON string
            [
              { "command": "getData",
                "args": {
                  "range": "Blog!A16:F16"
                }
              }
            ]
        env:
          GSHEET_CLIENT_EMAIL: ${{ secrets.GSHEET_CLIENT_EMAIL }}
          GSHEET_PRIVATE_KEY: ${{ secrets.GSHEET_PRIVATE_KEY }}

      - id: 'update_worksheet_17'
        uses: jroehl/gsheet.action@release # you can specify '@release' to always have the latest changes
        with:
          spreadsheetId: ${{ secrets.GSHEET_ID }}
          commands: | # list of commands, specified as a valid JSON string
            [
              { "command": "getData",
                "args": {
                  "range": "Blog!A17:F17"
                }
              }
            ]
        env:
          GSHEET_CLIENT_EMAIL: ${{ secrets.GSHEET_CLIENT_EMAIL }}
          GSHEET_PRIVATE_KEY: ${{ secrets.GSHEET_PRIVATE_KEY }}

      - id: 'update_worksheet_18'
        uses: jroehl/gsheet.action@release # you can specify '@release' to always have the latest changes
        with:
          spreadsheetId: ${{ secrets.GSHEET_ID }}
          commands: | # list of commands, specified as a valid JSON string
            [
              { "command": "getData",
                "args": {
                  "range": "Blog!A18:F18"
                }
              }
            ]
        env:
          GSHEET_CLIENT_EMAIL: ${{ secrets.GSHEET_CLIENT_EMAIL }}
          GSHEET_PRIVATE_KEY: ${{ secrets.GSHEET_PRIVATE_KEY }}

      - id: 'update_worksheet_19'
        uses: jroehl/gsheet.action@release # you can specify '@release' to always have the latest changes
        with:
          spreadsheetId: ${{ secrets.GSHEET_ID }}
          commands: | # list of commands, specified as a valid JSON string
            [
              { "command": "getData",
                "args": {
                  "range": "Blog!A19:F19"
                }
              }
            ]
        env:
          GSHEET_CLIENT_EMAIL: ${{ secrets.GSHEET_CLIENT_EMAIL }}
          GSHEET_PRIVATE_KEY: ${{ secrets.GSHEET_PRIVATE_KEY }}

      - id: 'update_worksheet_20'
        uses: jroehl/gsheet.action@release # you can specify '@release' to always have the latest changes
        with:
          spreadsheetId: ${{ secrets.GSHEET_ID }}
          commands: | # list of commands, specified as a valid JSON string
            [
              { "command": "getData",
                "args": {
                  "range": "Blog!A20:F20"
                }
              }
            ]
        env:
          GSHEET_CLIENT_EMAIL: ${{ secrets.GSHEET_CLIENT_EMAIL }}
          GSHEET_PRIVATE_KEY: ${{ secrets.GSHEET_PRIVATE_KEY }}

      - id: 'update_worksheet_21'
        uses: jroehl/gsheet.action@release # you can specify '@release' to always have the latest changes
        with:
          spreadsheetId: ${{ secrets.GSHEET_ID }}
          commands: | # list of commands, specified as a valid JSON string
            [
              { "command": "getData",
                "args": {
                  "range": "Blog!A21:F21"
                }
              }
            ]
        env:
          GSHEET_CLIENT_EMAIL: ${{ secrets.GSHEET_CLIENT_EMAIL }}
          GSHEET_PRIVATE_KEY: ${{ secrets.GSHEET_PRIVATE_KEY }}

      - id: 'update_worksheet_22'
        uses: jroehl/gsheet.action@release # you can specify '@release' to always have the latest changes
        with:
          spreadsheetId: ${{ secrets.GSHEET_ID }}
          commands: | # list of commands, specified as a valid JSON string
            [
              { "command": "getData",
                "args": {
                  "range": "Blog!A22:F22"
                }
              }
            ]
        env:
          GSHEET_CLIENT_EMAIL: ${{ secrets.GSHEET_CLIENT_EMAIL }}
          GSHEET_PRIVATE_KEY: ${{ secrets.GSHEET_PRIVATE_KEY }}

      - id: 'update_worksheet_23'
        uses: jroehl/gsheet.action@release # you can specify '@release' to always have the latest changes
        with:
          spreadsheetId: ${{ secrets.GSHEET_ID }}
          commands: | # list of commands, specified as a valid JSON string
            [
              { "command": "getData",
                "args": {
                  "range": "Blog!A23:F23"
                }
              }
            ]
        env:
          GSHEET_CLIENT_EMAIL: ${{ secrets.GSHEET_CLIENT_EMAIL }}
          GSHEET_PRIVATE_KEY: ${{ secrets.GSHEET_PRIVATE_KEY }}

      - id: 'update_worksheet_24'
        uses: jroehl/gsheet.action@release # you can specify '@release' to always have the latest changes
        with:
          spreadsheetId: ${{ secrets.GSHEET_ID }}
          commands: | # list of commands, specified as a valid JSON string
            [
              { "command": "getData",
                "args": {
                  "range": "Blog!A24:F24"
                }
              }
            ]
        env:
          GSHEET_CLIENT_EMAIL: ${{ secrets.GSHEET_CLIENT_EMAIL }}
          GSHEET_PRIVATE_KEY: ${{ secrets.GSHEET_PRIVATE_KEY }}

      - id: 'update_worksheet_25'
        uses: jroehl/gsheet.action@release # you can specify '@release' to always have the latest changes
        with:
          spreadsheetId: ${{ secrets.GSHEET_ID }}
          commands: | # list of commands, specified as a valid JSON string
            [
              { "command": "getData",
                "args": {
                  "range": "Blog!A25:F25"
                }
              }
            ]
        env:
          GSHEET_CLIENT_EMAIL: ${{ secrets.GSHEET_CLIENT_EMAIL }}
          GSHEET_PRIVATE_KEY: ${{ secrets.GSHEET_PRIVATE_KEY }}

      - id: 'update_worksheet_26'
        uses: jroehl/gsheet.action@release # you can specify '@release' to always have the latest changes
        with:
          spreadsheetId: ${{ secrets.GSHEET_ID }}
          commands: | # list of commands, specified as a valid JSON string
            [
              { "command": "getData",
                "args": {
                  "range": "Blog!A26:F26"
                }
              }
            ]
        env:
          GSHEET_CLIENT_EMAIL: ${{ secrets.GSHEET_CLIENT_EMAIL }}
          GSHEET_PRIVATE_KEY: ${{ secrets.GSHEET_PRIVATE_KEY }}

      - id: 'update_worksheet_27'
        uses: jroehl/gsheet.action@release # you can specify '@release' to always have the latest changes
        with:
          spreadsheetId: ${{ secrets.GSHEET_ID }}
          commands: | # list of commands, specified as a valid JSON string
            [
              { "command": "getData",
                "args": {
                  "range": "Blog!A27:F27"
                }
              }
            ]
        env:
          GSHEET_CLIENT_EMAIL: ${{ secrets.GSHEET_CLIENT_EMAIL }}
          GSHEET_PRIVATE_KEY: ${{ secrets.GSHEET_PRIVATE_KEY }}

      - id: 'update_worksheet_28'
        uses: jroehl/gsheet.action@release # you can specify '@release' to always have the latest changes
        with:
          spreadsheetId: ${{ secrets.GSHEET_ID }}
          commands: | # list of commands, specified as a valid JSON string
            [
              { "command": "getData",
                "args": {
                  "range": "Blog!A28:F28"
                }
              }
            ]
        env:
          GSHEET_CLIENT_EMAIL: ${{ secrets.GSHEET_CLIENT_EMAIL }}
          GSHEET_PRIVATE_KEY: ${{ secrets.GSHEET_PRIVATE_KEY }}

      - id: 'update_worksheet_29'
        uses: jroehl/gsheet.action@release # you can specify '@release' to always have the latest changes
        with:
          spreadsheetId: ${{ secrets.GSHEET_ID }}
          commands: | # list of commands, specified as a valid JSON string
            [
              { "command": "getData",
                "args": {
                  "range": "Blog!A29:F29"
                }
              }
            ]
        env:
          GSHEET_CLIENT_EMAIL: ${{ secrets.GSHEET_CLIENT_EMAIL }}
          GSHEET_PRIVATE_KEY: ${{ secrets.GSHEET_PRIVATE_KEY }}

      - id: 'update_worksheet_30'
        uses: jroehl/gsheet.action@release # you can specify '@release' to always have the latest changes
        with:
          spreadsheetId: ${{ secrets.GSHEET_ID }}
          commands: | # list of commands, specified as a valid JSON string
            [
              { "command": "getData",
                "args": {
                  "range": "Blog!A30:F30"
                }
              }
            ]
        env:
          GSHEET_CLIENT_EMAIL: ${{ secrets.GSHEET_CLIENT_EMAIL }}
          GSHEET_PRIVATE_KEY: ${{ secrets.GSHEET_PRIVATE_KEY }}

      - id: 'update_worksheet_31'
        uses: jroehl/gsheet.action@release # you can specify '@release' to always have the latest changes
        with:
          spreadsheetId: ${{ secrets.GSHEET_ID }}
          commands: | # list of commands, specified as a valid JSON string
            [
              { "command": "getData",
                "args": {
                  "range": "Blog!A31:F31"
                }
              }
            ]
        env:
          GSHEET_CLIENT_EMAIL: ${{ secrets.GSHEET_CLIENT_EMAIL }}
          GSHEET_PRIVATE_KEY: ${{ secrets.GSHEET_PRIVATE_KEY }}

      - id: 'update_worksheet_32'
        uses: jroehl/gsheet.action@release # you can specify '@release' to always have the latest changes
        with:
          spreadsheetId: ${{ secrets.GSHEET_ID }}
          commands: | # list of commands, specified as a valid JSON string
            [
              { "command": "getData",
                "args": {
                  "range": "Blog!A32:F32"
                }
              }
            ]
        env:
          GSHEET_CLIENT_EMAIL: ${{ secrets.GSHEET_CLIENT_EMAIL }}
          GSHEET_PRIVATE_KEY: ${{ secrets.GSHEET_PRIVATE_KEY }}

      - id: 'update_worksheet_33'
        uses: jroehl/gsheet.action@release # you can specify '@release' to always have the latest changes
        with:
          spreadsheetId: ${{ secrets.GSHEET_ID }}
          commands: | # list of commands, specified as a valid JSON string
            [
              { "command": "getData",
                "args": {
                  "range": "Blog!A33:F33"
                }
              }
            ]
        env:
          GSHEET_CLIENT_EMAIL: ${{ secrets.GSHEET_CLIENT_EMAIL }}
          GSHEET_PRIVATE_KEY: ${{ secrets.GSHEET_PRIVATE_KEY }}

      - id: 'update_worksheet_34'
        uses: jroehl/gsheet.action@release # you can specify '@release' to always have the latest changes
        with:
          spreadsheetId: ${{ secrets.GSHEET_ID }}
          commands: | # list of commands, specified as a valid JSON string
            [
              { "command": "getData",
                "args": {
                  "range": "Blog!A34:F34"
                }
              }
            ]
        env:
          GSHEET_CLIENT_EMAIL: ${{ secrets.GSHEET_CLIENT_EMAIL }}
          GSHEET_PRIVATE_KEY: ${{ secrets.GSHEET_PRIVATE_KEY }}

      - id: 'update_worksheet_35'
        uses: jroehl/gsheet.action@release # you can specify '@release' to always have the latest changes
        with:
          spreadsheetId: ${{ secrets.GSHEET_ID }}
          commands: | # list of commands, specified as a valid JSON string
            [
              { "command": "getData",
                "args": {
                  "range": "Blog!A35:F35"
                }
              }
            ]
        env:
          GSHEET_CLIENT_EMAIL: ${{ secrets.GSHEET_CLIENT_EMAIL }}
          GSHEET_PRIVATE_KEY: ${{ secrets.GSHEET_PRIVATE_KEY }}

      - id: 'update_worksheet_36'
        uses: jroehl/gsheet.action@release # you can specify '@release' to always have the latest changes
        with:
          spreadsheetId: ${{ secrets.GSHEET_ID }}
          commands: | # list of commands, specified as a valid JSON string
            [
              { "command": "getData",
                "args": {
                  "range": "Blog!A36:F36"
                }
              }
            ]
        env:
          GSHEET_CLIENT_EMAIL: ${{ secrets.GSHEET_CLIENT_EMAIL }}
          GSHEET_PRIVATE_KEY: ${{ secrets.GSHEET_PRIVATE_KEY }}

      - id: 'update_worksheet_37'
        uses: jroehl/gsheet.action@release # you can specify '@release' to always have the latest changes
        with:
          spreadsheetId: ${{ secrets.GSHEET_ID }}
          commands: | # list of commands, specified as a valid JSON string
            [
              { "command": "getData",
                "args": {
                  "range": "Blog!A37:F37"
                }
              }
            ]
        env:
          GSHEET_CLIENT_EMAIL: ${{ secrets.GSHEET_CLIENT_EMAIL }}
          GSHEET_PRIVATE_KEY: ${{ secrets.GSHEET_PRIVATE_KEY }}

      - id: 'update_worksheet_38'
        uses: jroehl/gsheet.action@release # you can specify '@release' to always have the latest changes
        with:
          spreadsheetId: ${{ secrets.GSHEET_ID }}
          commands: | # list of commands, specified as a valid JSON string
            [
              { "command": "getData",
                "args": {
                  "range": "Blog!A38:F38"
                }
              }
            ]
        env:
          GSHEET_CLIENT_EMAIL: ${{ secrets.GSHEET_CLIENT_EMAIL }}
          GSHEET_PRIVATE_KEY: ${{ secrets.GSHEET_PRIVATE_KEY }}

      - id: 'update_worksheet_39'
        uses: jroehl/gsheet.action@release # you can specify '@release' to always have the latest changes
        with:
          spreadsheetId: ${{ secrets.GSHEET_ID }}
          commands: | # list of commands, specified as a valid JSON string
            [
              { "command": "getData",
                "args": {
                  "range": "Blog!A39:F39"
                }
              }
            ]
        env:
          GSHEET_CLIENT_EMAIL: ${{ secrets.GSHEET_CLIENT_EMAIL }}
          GSHEET_PRIVATE_KEY: ${{ secrets.GSHEET_PRIVATE_KEY }}

      - id: 'update_worksheet_40'
        uses: jroehl/gsheet.action@release # you can specify '@release' to always have the latest changes
        with:
          spreadsheetId: ${{ secrets.GSHEET_ID }}
          commands: | # list of commands, specified as a valid JSON string
            [
              { "command": "getData",
                "args": {
                  "range": "Blog!A40:F40"
                }
              }
            ]
        env:
          GSHEET_CLIENT_EMAIL: ${{ secrets.GSHEET_CLIENT_EMAIL }}
          GSHEET_PRIVATE_KEY: ${{ secrets.GSHEET_PRIVATE_KEY }}

      - id: 'update_worksheet_41'
        uses: jroehl/gsheet.action@release # you can specify '@release' to always have the latest changes
        with:
          spreadsheetId: ${{ secrets.GSHEET_ID }}
          commands: | # list of commands, specified as a valid JSON string
            [
              { "command": "getData",
                "args": {
                  "range": "Blog!A41:F41"
                }
              }
            ]
        env:
          GSHEET_CLIENT_EMAIL: ${{ secrets.GSHEET_CLIENT_EMAIL }}
          GSHEET_PRIVATE_KEY: ${{ secrets.GSHEET_PRIVATE_KEY }}

      - id: 'update_worksheet_42'
        uses: jroehl/gsheet.action@release # you can specify '@release' to always have the latest changes
        with:
          spreadsheetId: ${{ secrets.GSHEET_ID }}
          commands: | # list of commands, specified as a valid JSON string
            [
              { "command": "getData",
                "args": {
                  "range": "Blog!A42:F42"
                }
              }
            ]
        env:
          GSHEET_CLIENT_EMAIL: ${{ secrets.GSHEET_CLIENT_EMAIL }}
          GSHEET_PRIVATE_KEY: ${{ secrets.GSHEET_PRIVATE_KEY }}

      - id: 'update_worksheet_43'
        uses: jroehl/gsheet.action@release # you can specify '@release' to always have the latest changes
        with:
          spreadsheetId: ${{ secrets.GSHEET_ID }}
          commands: | # list of commands, specified as a valid JSON string
            [
              { "command": "getData",
                "args": {
                  "range": "Blog!A43:F43"
                }
              }
            ]
        env:
          GSHEET_CLIENT_EMAIL: ${{ secrets.GSHEET_CLIENT_EMAIL }}
          GSHEET_PRIVATE_KEY: ${{ secrets.GSHEET_PRIVATE_KEY }}

      - id: 'update_worksheet_44'
        uses: jroehl/gsheet.action@release # you can specify '@release' to always have the latest changes
        with:
          spreadsheetId: ${{ secrets.GSHEET_ID }}
          commands: | # list of commands, specified as a valid JSON string
            [
              { "command": "getData",
                "args": {
                  "range": "Blog!A44:F44"
                }
              }
            ]
        env:
          GSHEET_CLIENT_EMAIL: ${{ secrets.GSHEET_CLIENT_EMAIL }}
          GSHEET_PRIVATE_KEY: ${{ secrets.GSHEET_PRIVATE_KEY }}

      - id: 'update_worksheet_45'
        uses: jroehl/gsheet.action@release # you can specify '@release' to always have the latest changes
        with:
          spreadsheetId: ${{ secrets.GSHEET_ID }}
          commands: | # list of commands, specified as a valid JSON string
            [
              { "command": "getData",
                "args": {
                  "range": "Blog!A45:F45"
                }
              }
            ]
        env:
          GSHEET_CLIENT_EMAIL: ${{ secrets.GSHEET_CLIENT_EMAIL }}
          GSHEET_PRIVATE_KEY: ${{ secrets.GSHEET_PRIVATE_KEY }}

      - id: 'update_worksheet_46'
        uses: jroehl/gsheet.action@release # you can specify '@release' to always have the latest changes
        with:
          spreadsheetId: ${{ secrets.GSHEET_ID }}
          commands: | # list of commands, specified as a valid JSON string
            [
              { "command": "getData",
                "args": {
                  "range": "Blog!A46:F46"
                }
              }
            ]
        env:
          GSHEET_CLIENT_EMAIL: ${{ secrets.GSHEET_CLIENT_EMAIL }}
          GSHEET_PRIVATE_KEY: ${{ secrets.GSHEET_PRIVATE_KEY }}

      - id: 'update_worksheet_47'
        uses: jroehl/gsheet.action@release # you can specify '@release' to always have the latest changes
        with:
          spreadsheetId: ${{ secrets.GSHEET_ID }}
          commands: | # list of commands, specified as a valid JSON string
            [
              { "command": "getData",
                "args": {
                  "range": "Blog!A47:F47"
                }
              }
            ]
        env:
          GSHEET_CLIENT_EMAIL: ${{ secrets.GSHEET_CLIENT_EMAIL }}
          GSHEET_PRIVATE_KEY: ${{ secrets.GSHEET_PRIVATE_KEY }}

      - id: 'update_worksheet_48'
        uses: jroehl/gsheet.action@release # you can specify '@release' to always have the latest changes
        with:
          spreadsheetId: ${{ secrets.GSHEET_ID }}
          commands: | # list of commands, specified as a valid JSON string
            [
              { "command": "getData",
                "args": {
                  "range": "Blog!A48:F48"
                }
              }
            ]
        env:
          GSHEET_CLIENT_EMAIL: ${{ secrets.GSHEET_CLIENT_EMAIL }}
          GSHEET_PRIVATE_KEY: ${{ secrets.GSHEET_PRIVATE_KEY }}

      - id: 'update_worksheet_49'
        uses: jroehl/gsheet.action@release # you can specify '@release' to always have the latest changes
        with:
          spreadsheetId: ${{ secrets.GSHEET_ID }}
          commands: | # list of commands, specified as a valid JSON string
            [
              { "command": "getData",
                "args": {
                  "range": "Blog!A49:F49"
                }
              }
            ]
        env:
          GSHEET_CLIENT_EMAIL: ${{ secrets.GSHEET_CLIENT_EMAIL }}
          GSHEET_PRIVATE_KEY: ${{ secrets.GSHEET_PRIVATE_KEY }}

      - id: 'update_worksheet_50'
        uses: jroehl/gsheet.action@release # you can specify '@release' to always have the latest changes
        with:
          spreadsheetId: ${{ secrets.GSHEET_ID }}
          commands: | # list of commands, specified as a valid JSON string
            [
              { "command": "getData",
                "args": {
                  "range": "Blog!A50:F50"
                }
              }
            ]
        env:
          GSHEET_CLIENT_EMAIL: ${{ secrets.GSHEET_CLIENT_EMAIL }}
          GSHEET_PRIVATE_KEY: ${{ secrets.GSHEET_PRIVATE_KEY }}

      - id: 'update_worksheet_51'
        uses: jroehl/gsheet.action@release # you can specify '@release' to always have the latest changes
        with:
          spreadsheetId: ${{ secrets.GSHEET_ID }}
          commands: | # list of commands, specified as a valid JSON string
            [
              { "command": "getData",
                "args": {
                  "range": "Blog!A51:F51"
                }
              }
            ]
        env:
          GSHEET_CLIENT_EMAIL: ${{ secrets.GSHEET_CLIENT_EMAIL }}
          GSHEET_PRIVATE_KEY: ${{ secrets.GSHEET_PRIVATE_KEY }}

      - id: 'update_worksheet_52'
        uses: jroehl/gsheet.action@release # you can specify '@release' to always have the latest changes
        with:
          spreadsheetId: ${{ secrets.GSHEET_ID }}
          commands: | # list of commands, specified as a valid JSON string
            [
              { "command": "getData",
                "args": {
                  "range": "Blog!A52:F52"
                }
              }
            ]
        env:
          GSHEET_CLIENT_EMAIL: ${{ secrets.GSHEET_CLIENT_EMAIL }}
          GSHEET_PRIVATE_KEY: ${{ secrets.GSHEET_PRIVATE_KEY }}

      - id: 'update_worksheet_53'
        uses: jroehl/gsheet.action@release # you can specify '@release' to always have the latest changes
        with:
          spreadsheetId: ${{ secrets.GSHEET_ID }}
          commands: | # list of commands, specified as a valid JSON string
            [
              { "command": "getData",
                "args": {
                  "range": "Blog!A53:F53"
                }
              }
            ]
        env:
          GSHEET_CLIENT_EMAIL: ${{ secrets.GSHEET_CLIENT_EMAIL }}
          GSHEET_PRIVATE_KEY: ${{ secrets.GSHEET_PRIVATE_KEY }}

      - id: 'update_worksheet_54'
        uses: jroehl/gsheet.action@release # you can specify '@release' to always have the latest changes
        with:
          spreadsheetId: ${{ secrets.GSHEET_ID }}
          commands: | # list of commands, specified as a valid JSON string
            [
              { "command": "getData",
                "args": {
                  "range": "Blog!A54:F54"
                }
              }
            ]
        env:
          GSHEET_CLIENT_EMAIL: ${{ secrets.GSHEET_CLIENT_EMAIL }}
          GSHEET_PRIVATE_KEY: ${{ secrets.GSHEET_PRIVATE_KEY }}

      - id: 'update_worksheet_55'
        uses: jroehl/gsheet.action@release # you can specify '@release' to always have the latest changes
        with:
          spreadsheetId: ${{ secrets.GSHEET_ID }}
          commands: | # list of commands, specified as a valid JSON string
            [
              { "command": "getData",
                "args": {
                  "range": "Blog!A55:F55"
                }
              }
            ]
        env:
          GSHEET_CLIENT_EMAIL: ${{ secrets.GSHEET_CLIENT_EMAIL }}
          GSHEET_PRIVATE_KEY: ${{ secrets.GSHEET_PRIVATE_KEY }}

      - id: 'update_worksheet_56'
        uses: jroehl/gsheet.action@release # you can specify '@release' to always have the latest changes
        with:
          spreadsheetId: ${{ secrets.GSHEET_ID }}
          commands: | # list of commands, specified as a valid JSON string
            [
              { "command": "getData",
                "args": {
                  "range": "Blog!A56:F56"
                }
              }
            ]
        env:
          GSHEET_CLIENT_EMAIL: ${{ secrets.GSHEET_CLIENT_EMAIL }}
          GSHEET_PRIVATE_KEY: ${{ secrets.GSHEET_PRIVATE_KEY }}

      - id: 'update_worksheet_57'
        uses: jroehl/gsheet.action@release # you can specify '@release' to always have the latest changes
        with:
          spreadsheetId: ${{ secrets.GSHEET_ID }}
          commands: | # list of commands, specified as a valid JSON string
            [
              { "command": "getData",
                "args": {
                  "range": "Blog!A57:F57"
                }
              }
            ]
        env:
          GSHEET_CLIENT_EMAIL: ${{ secrets.GSHEET_CLIENT_EMAIL }}
          GSHEET_PRIVATE_KEY: ${{ secrets.GSHEET_PRIVATE_KEY }}

      - id: 'update_worksheet_58'
        uses: jroehl/gsheet.action@release # you can specify '@release' to always have the latest changes
        with:
          spreadsheetId: ${{ secrets.GSHEET_ID }}
          commands: | # list of commands, specified as a valid JSON string
            [
              { "command": "getData",
                "args": {
                  "range": "Blog!A58:F58"
                }
              }
            ]
        env:
          GSHEET_CLIENT_EMAIL: ${{ secrets.GSHEET_CLIENT_EMAIL }}
          GSHEET_PRIVATE_KEY: ${{ secrets.GSHEET_PRIVATE_KEY }}

      - id: 'update_worksheet_59'
        uses: jroehl/gsheet.action@release # you can specify '@release' to always have the latest changes
        with:
          spreadsheetId: ${{ secrets.GSHEET_ID }}
          commands: | # list of commands, specified as a valid JSON string
            [
              { "command": "getData",
                "args": {
                  "range": "Blog!A59:F59"
                }
              }
            ]
        env:
          GSHEET_CLIENT_EMAIL: ${{ secrets.GSHEET_CLIENT_EMAIL }}
          GSHEET_PRIVATE_KEY: ${{ secrets.GSHEET_PRIVATE_KEY }}

      - id: 'update_worksheet_60'
        uses: jroehl/gsheet.action@release # you can specify '@release' to always have the latest changes
        with:
          spreadsheetId: ${{ secrets.GSHEET_ID }}
          commands: | # list of commands, specified as a valid JSON string
            [
              { "command": "getData",
                "args": {
                  "range": "Blog!A60:F60"
                }
              }
            ]
        env:
          GSHEET_CLIENT_EMAIL: ${{ secrets.GSHEET_CLIENT_EMAIL }}
          GSHEET_PRIVATE_KEY: ${{ secrets.GSHEET_PRIVATE_KEY }}

      - id: 'update_worksheet_61'
        uses: jroehl/gsheet.action@release # you can specify '@release' to always have the latest changes
        with:
          spreadsheetId: ${{ secrets.GSHEET_ID }}
          commands: | # list of commands, specified as a valid JSON string
            [
              { "command": "getData",
                "args": {
                  "range": "Blog!A61:F61"
                }
              }
            ]
        env:
          GSHEET_CLIENT_EMAIL: ${{ secrets.GSHEET_CLIENT_EMAIL }}
          GSHEET_PRIVATE_KEY: ${{ secrets.GSHEET_PRIVATE_KEY }}

      - id: 'update_worksheet_62'
        uses: jroehl/gsheet.action@release # you can specify '@release' to always have the latest changes
        with:
          spreadsheetId: ${{ secrets.GSHEET_ID }}
          commands: | # list of commands, specified as a valid JSON string
            [
              { "command": "getData",
                "args": {
                  "range": "Blog!A62:F62"
                }
              }
            ]
        env:
          GSHEET_CLIENT_EMAIL: ${{ secrets.GSHEET_CLIENT_EMAIL }}
          GSHEET_PRIVATE_KEY: ${{ secrets.GSHEET_PRIVATE_KEY }}

      - id: 'update_worksheet_63'
        uses: jroehl/gsheet.action@release # you can specify '@release' to always have the latest changes
        with:
          spreadsheetId: ${{ secrets.GSHEET_ID }}
          commands: | # list of commands, specified as a valid JSON string
            [
              { "command": "getData",
                "args": {
                  "range": "Blog!A63:F63"
                }
              }
            ]
        env:
          GSHEET_CLIENT_EMAIL: ${{ secrets.GSHEET_CLIENT_EMAIL }}
          GSHEET_PRIVATE_KEY: ${{ secrets.GSHEET_PRIVATE_KEY }}

      - id: 'update_worksheet_64'
        uses: jroehl/gsheet.action@release # you can specify '@release' to always have the latest changes
        with:
          spreadsheetId: ${{ secrets.GSHEET_ID }}
          commands: | # list of commands, specified as a valid JSON string
            [
              { "command": "getData",
                "args": {
                  "range": "Blog!A64:F64"
                }
              }
            ]
        env:
          GSHEET_CLIENT_EMAIL: ${{ secrets.GSHEET_CLIENT_EMAIL }}
          GSHEET_PRIVATE_KEY: ${{ secrets.GSHEET_PRIVATE_KEY }}

      - id: 'update_worksheet_65'
        uses: jroehl/gsheet.action@release # you can specify '@release' to always have the latest changes
        with:
          spreadsheetId: ${{ secrets.GSHEET_ID }}
          commands: | # list of commands, specified as a valid JSON string
            [
              { "command": "getData",
                "args": {
                  "range": "Blog!A65:F65"
                }
              }
            ]
        env:
          GSHEET_CLIENT_EMAIL: ${{ secrets.GSHEET_CLIENT_EMAIL }}
          GSHEET_PRIVATE_KEY: ${{ secrets.GSHEET_PRIVATE_KEY }}

      - id: 'update_worksheet_66'
        uses: jroehl/gsheet.action@release # you can specify '@release' to always have the latest changes
        with:
          spreadsheetId: ${{ secrets.GSHEET_ID }}
          commands: | # list of commands, specified as a valid JSON string
            [
              { "command": "getData",
                "args": {
                  "range": "Blog!A66:F66"
                }
              }
            ]
        env:
          GSHEET_CLIENT_EMAIL: ${{ secrets.GSHEET_CLIENT_EMAIL }}
          GSHEET_PRIVATE_KEY: ${{ secrets.GSHEET_PRIVATE_KEY }}

      - id: 'update_worksheet_67'
        uses: jroehl/gsheet.action@release # you can specify '@release' to always have the latest changes
        with:
          spreadsheetId: ${{ secrets.GSHEET_ID }}
          commands: | # list of commands, specified as a valid JSON string
            [
              { "command": "getData",
                "args": {
                  "range": "Blog!A67:F67"
                }
              }
            ]
        env:
          GSHEET_CLIENT_EMAIL: ${{ secrets.GSHEET_CLIENT_EMAIL }}
          GSHEET_PRIVATE_KEY: ${{ secrets.GSHEET_PRIVATE_KEY }}

      - id: 'update_worksheet_68'
        uses: jroehl/gsheet.action@release # you can specify '@release' to always have the latest changes
        with:
          spreadsheetId: ${{ secrets.GSHEET_ID }}
          commands: | # list of commands, specified as a valid JSON string
            [
              { "command": "getData",
                "args": {
                  "range": "Blog!A68:F68"
                }
              }
            ]
        env:
          GSHEET_CLIENT_EMAIL: ${{ secrets.GSHEET_CLIENT_EMAIL }}
          GSHEET_PRIVATE_KEY: ${{ secrets.GSHEET_PRIVATE_KEY }}

      - id: 'update_worksheet_69'
        uses: jroehl/gsheet.action@release # you can specify '@release' to always have the latest changes
        with:
          spreadsheetId: ${{ secrets.GSHEET_ID }}
          commands: | # list of commands, specified as a valid JSON string
            [
              { "command": "getData",
                "args": {
                  "range": "Blog!A69:F69"
                }
              }
            ]
        env:
          GSHEET_CLIENT_EMAIL: ${{ secrets.GSHEET_CLIENT_EMAIL }}
          GSHEET_PRIVATE_KEY: ${{ secrets.GSHEET_PRIVATE_KEY }}

      - id: 'update_worksheet_70'
        uses: jroehl/gsheet.action@release # you can specify '@release' to always have the latest changes
        with:
          spreadsheetId: ${{ secrets.GSHEET_ID }}
          commands: | # list of commands, specified as a valid JSON string
            [
              { "command": "getData",
                "args": {
                  "range": "Blog!A70:F70"
                }
              }
            ]
        env:
          GSHEET_CLIENT_EMAIL: ${{ secrets.GSHEET_CLIENT_EMAIL }}
          GSHEET_PRIVATE_KEY: ${{ secrets.GSHEET_PRIVATE_KEY }}

      - id: 'update_worksheet_71'
        uses: jroehl/gsheet.action@release # you can specify '@release' to always have the latest changes
        with:
          spreadsheetId: ${{ secrets.GSHEET_ID }}
          commands: | # list of commands, specified as a valid JSON string
            [
              { "command": "getData",
                "args": {
                  "range": "Blog!A71:F71"
                }
              }
            ]
        env:
          GSHEET_CLIENT_EMAIL: ${{ secrets.GSHEET_CLIENT_EMAIL }}
          GSHEET_PRIVATE_KEY: ${{ secrets.GSHEET_PRIVATE_KEY }}

      - id: 'update_worksheet_72'
        uses: jroehl/gsheet.action@release # you can specify '@release' to always have the latest changes
        with:
          spreadsheetId: ${{ secrets.GSHEET_ID }}
          commands: | # list of commands, specified as a valid JSON string
            [
              { "command": "getData",
                "args": {
                  "range": "Blog!A72:F72"
                }
              }
            ]
        env:
          GSHEET_CLIENT_EMAIL: ${{ secrets.GSHEET_CLIENT_EMAIL }}
          GSHEET_PRIVATE_KEY: ${{ secrets.GSHEET_PRIVATE_KEY }}

      - id: 'update_worksheet_73'
        uses: jroehl/gsheet.action@release # you can specify '@release' to always have the latest changes
        with:
          spreadsheetId: ${{ secrets.GSHEET_ID }}
          commands: | # list of commands, specified as a valid JSON string
            [
              { "command": "getData",
                "args": {
                  "range": "Blog!A73:F73"
                }
              }
            ]
        env:
          GSHEET_CLIENT_EMAIL: ${{ secrets.GSHEET_CLIENT_EMAIL }}
          GSHEET_PRIVATE_KEY: ${{ secrets.GSHEET_PRIVATE_KEY }}

      - id: 'update_worksheet_74'
        uses: jroehl/gsheet.action@release # you can specify '@release' to always have the latest changes
        with:
          spreadsheetId: ${{ secrets.GSHEET_ID }}
          commands: | # list of commands, specified as a valid JSON string
            [
              { "command": "getData",
                "args": {
                  "range": "Blog!A74:F74"
                }
              }
            ]
        env:
          GSHEET_CLIENT_EMAIL: ${{ secrets.GSHEET_CLIENT_EMAIL }}
          GSHEET_PRIVATE_KEY: ${{ secrets.GSHEET_PRIVATE_KEY }}

      - id: 'update_worksheet_75'
        uses: jroehl/gsheet.action@release # you can specify '@release' to always have the latest changes
        with:
          spreadsheetId: ${{ secrets.GSHEET_ID }}
          commands: | # list of commands, specified as a valid JSON string
            [
              { "command": "getData",
                "args": {
                  "range": "Blog!A75:F75"
                }
              }
            ]
        env:
          GSHEET_CLIENT_EMAIL: ${{ secrets.GSHEET_CLIENT_EMAIL }}
          GSHEET_PRIVATE_KEY: ${{ secrets.GSHEET_PRIVATE_KEY }}

      - id: 'update_worksheet_76'
        uses: jroehl/gsheet.action@release # you can specify '@release' to always have the latest changes
        with:
          spreadsheetId: ${{ secrets.GSHEET_ID }}
          commands: | # list of commands, specified as a valid JSON string
            [
              { "command": "getData",
                "args": {
                  "range": "Blog!A76:F76"
                }
              }
            ]
        env:
          GSHEET_CLIENT_EMAIL: ${{ secrets.GSHEET_CLIENT_EMAIL }}
          GSHEET_PRIVATE_KEY: ${{ secrets.GSHEET_PRIVATE_KEY }}

      - id: 'update_worksheet_77'
        uses: jroehl/gsheet.action@release # you can specify '@release' to always have the latest changes
        with:
          spreadsheetId: ${{ secrets.GSHEET_ID }}
          commands: | # list of commands, specified as a valid JSON string
            [
              { "command": "getData",
                "args": {
                  "range": "Blog!A77:F77"
                }
              }
            ]
        env:
          GSHEET_CLIENT_EMAIL: ${{ secrets.GSHEET_CLIENT_EMAIL }}
          GSHEET_PRIVATE_KEY: ${{ secrets.GSHEET_PRIVATE_KEY }}

      - id: 'update_worksheet_78'
        uses: jroehl/gsheet.action@release # you can specify '@release' to always have the latest changes
        with:
          spreadsheetId: ${{ secrets.GSHEET_ID }}
          commands: | # list of commands, specified as a valid JSON string
            [
              { "command": "getData",
                "args": {
                  "range": "Blog!A78:F78"
                }
              }
            ]
        env:
          GSHEET_CLIENT_EMAIL: ${{ secrets.GSHEET_CLIENT_EMAIL }}
          GSHEET_PRIVATE_KEY: ${{ secrets.GSHEET_PRIVATE_KEY }}

      - id: 'update_worksheet_79'
        uses: jroehl/gsheet.action@release # you can specify '@release' to always have the latest changes
        with:
          spreadsheetId: ${{ secrets.GSHEET_ID }}
          commands: | # list of commands, specified as a valid JSON string
            [
              { "command": "getData",
                "args": {
                  "range": "Blog!A79:F79"
                }
              }
            ]
        env:
          GSHEET_CLIENT_EMAIL: ${{ secrets.GSHEET_CLIENT_EMAIL }}
          GSHEET_PRIVATE_KEY: ${{ secrets.GSHEET_PRIVATE_KEY }}

      - id: 'update_worksheet_80'
        uses: jroehl/gsheet.action@release # you can specify '@release' to always have the latest changes
        with:
          spreadsheetId: ${{ secrets.GSHEET_ID }}
          commands: | # list of commands, specified as a valid JSON string
            [
              { "command": "getData",
                "args": {
                  "range": "Blog!A80:F80"
                }
              }
            ]
        env:
          GSHEET_CLIENT_EMAIL: ${{ secrets.GSHEET_CLIENT_EMAIL }}
          GSHEET_PRIVATE_KEY: ${{ secrets.GSHEET_PRIVATE_KEY }}

      - id: 'update_worksheet_81'
        uses: jroehl/gsheet.action@release # you can specify '@release' to always have the latest changes
        with:
          spreadsheetId: ${{ secrets.GSHEET_ID }}
          commands: | # list of commands, specified as a valid JSON string
            [
              { "command": "getData",
                "args": {
                  "range": "Blog!A81:F81"
                }
              }
            ]
        env:
          GSHEET_CLIENT_EMAIL: ${{ secrets.GSHEET_CLIENT_EMAIL }}
          GSHEET_PRIVATE_KEY: ${{ secrets.GSHEET_PRIVATE_KEY }}

      - id: 'update_worksheet_82'
        uses: jroehl/gsheet.action@release # you can specify '@release' to always have the latest changes
        with:
          spreadsheetId: ${{ secrets.GSHEET_ID }}
          commands: | # list of commands, specified as a valid JSON string
            [
              { "command": "getData",
                "args": {
                  "range": "Blog!A82:F82"
                }
              }
            ]
        env:
          GSHEET_CLIENT_EMAIL: ${{ secrets.GSHEET_CLIENT_EMAIL }}
          GSHEET_PRIVATE_KEY: ${{ secrets.GSHEET_PRIVATE_KEY }}

      - id: 'update_worksheet_83'
        uses: jroehl/gsheet.action@release # you can specify '@release' to always have the latest changes
        with:
          spreadsheetId: ${{ secrets.GSHEET_ID }}
          commands: | # list of commands, specified as a valid JSON string
            [
              { "command": "getData",
                "args": {
                  "range": "Blog!A83:F83"
                }
              }
            ]
        env:
          GSHEET_CLIENT_EMAIL: ${{ secrets.GSHEET_CLIENT_EMAIL }}
          GSHEET_PRIVATE_KEY: ${{ secrets.GSHEET_PRIVATE_KEY }}

      - id: 'update_worksheet_84'
        uses: jroehl/gsheet.action@release # you can specify '@release' to always have the latest changes
        with:
          spreadsheetId: ${{ secrets.GSHEET_ID }}
          commands: | # list of commands, specified as a valid JSON string
            [
              { "command": "getData",
                "args": {
                  "range": "Blog!A84:F84"
                }
              }
            ]
        env:
          GSHEET_CLIENT_EMAIL: ${{ secrets.GSHEET_CLIENT_EMAIL }}
          GSHEET_PRIVATE_KEY: ${{ secrets.GSHEET_PRIVATE_KEY }}

      - id: 'update_worksheet_85'
        uses: jroehl/gsheet.action@release # you can specify '@release' to always have the latest changes
        with:
          spreadsheetId: ${{ secrets.GSHEET_ID }}
          commands: | # list of commands, specified as a valid JSON string
            [
              { "command": "getData",
                "args": {
                  "range": "Blog!A85:F85"
                }
              }
            ]
        env:
          GSHEET_CLIENT_EMAIL: ${{ secrets.GSHEET_CLIENT_EMAIL }}
          GSHEET_PRIVATE_KEY: ${{ secrets.GSHEET_PRIVATE_KEY }}

      - id: 'update_worksheet_86'
        uses: jroehl/gsheet.action@release # you can specify '@release' to always have the latest changes
        with:
          spreadsheetId: ${{ secrets.GSHEET_ID }}
          commands: | # list of commands, specified as a valid JSON string
            [
              { "command": "getData",
                "args": {
                  "range": "Blog!A86:F86"
                }
              }
            ]
        env:
          GSHEET_CLIENT_EMAIL: ${{ secrets.GSHEET_CLIENT_EMAIL }}
          GSHEET_PRIVATE_KEY: ${{ secrets.GSHEET_PRIVATE_KEY }}

      - id: 'update_worksheet_87'
        uses: jroehl/gsheet.action@release # you can specify '@release' to always have the latest changes
        with:
          spreadsheetId: ${{ secrets.GSHEET_ID }}
          commands: | # list of commands, specified as a valid JSON string
            [
              { "command": "getData",
                "args": {
                  "range": "Blog!A87:F87"
                }
              }
            ]
        env:
          GSHEET_CLIENT_EMAIL: ${{ secrets.GSHEET_CLIENT_EMAIL }}
          GSHEET_PRIVATE_KEY: ${{ secrets.GSHEET_PRIVATE_KEY }}

      - id: 'update_worksheet_88'
        uses: jroehl/gsheet.action@release # you can specify '@release' to always have the latest changes
        with:
          spreadsheetId: ${{ secrets.GSHEET_ID }}
          commands: | # list of commands, specified as a valid JSON string
            [
              { "command": "getData",
                "args": {
                  "range": "Blog!A88:F88"
                }
              }
            ]
        env:
          GSHEET_CLIENT_EMAIL: ${{ secrets.GSHEET_CLIENT_EMAIL }}
          GSHEET_PRIVATE_KEY: ${{ secrets.GSHEET_PRIVATE_KEY }}

      - id: 'update_worksheet_89'
        uses: jroehl/gsheet.action@release # you can specify '@release' to always have the latest changes
        with:
          spreadsheetId: ${{ secrets.GSHEET_ID }}
          commands: | # list of commands, specified as a valid JSON string
            [
              { "command": "getData",
                "args": {
                  "range": "Blog!A89:F89"
                }
              }
            ]
        env:
          GSHEET_CLIENT_EMAIL: ${{ secrets.GSHEET_CLIENT_EMAIL }}
          GSHEET_PRIVATE_KEY: ${{ secrets.GSHEET_PRIVATE_KEY }}

      - id: 'update_worksheet_90'
        uses: jroehl/gsheet.action@release # you can specify '@release' to always have the latest changes
        with:
          spreadsheetId: ${{ secrets.GSHEET_ID }}
          commands: | # list of commands, specified as a valid JSON string
            [
              { "command": "getData",
                "args": {
                  "range": "Blog!A90:F90"
                }
              }
            ]
        env:
          GSHEET_CLIENT_EMAIL: ${{ secrets.GSHEET_CLIENT_EMAIL }}
          GSHEET_PRIVATE_KEY: ${{ secrets.GSHEET_PRIVATE_KEY }}

      - id: 'update_worksheet_91'
        uses: jroehl/gsheet.action@release # you can specify '@release' to always have the latest changes
        with:
          spreadsheetId: ${{ secrets.GSHEET_ID }}
          commands: | # list of commands, specified as a valid JSON string
            [
              { "command": "getData",
                "args": {
                  "range": "Blog!A91:F91"
                }
              }
            ]
        env:
          GSHEET_CLIENT_EMAIL: ${{ secrets.GSHEET_CLIENT_EMAIL }}
          GSHEET_PRIVATE_KEY: ${{ secrets.GSHEET_PRIVATE_KEY }}

      - id: 'update_worksheet_92'
        uses: jroehl/gsheet.action@release # you can specify '@release' to always have the latest changes
        with:
          spreadsheetId: ${{ secrets.GSHEET_ID }}
          commands: | # list of commands, specified as a valid JSON string
            [
              { "command": "getData",
                "args": {
                  "range": "Blog!A92:F92"
                }
              }
            ]
        env:
          GSHEET_CLIENT_EMAIL: ${{ secrets.GSHEET_CLIENT_EMAIL }}
          GSHEET_PRIVATE_KEY: ${{ secrets.GSHEET_PRIVATE_KEY }}

      - id: 'update_worksheet_93'
        uses: jroehl/gsheet.action@release # you can specify '@release' to always have the latest changes
        with:
          spreadsheetId: ${{ secrets.GSHEET_ID }}
          commands: | # list of commands, specified as a valid JSON string
            [
              { "command": "getData",
                "args": {
                  "range": "Blog!A93:F93"
                }
              }
            ]
        env:
          GSHEET_CLIENT_EMAIL: ${{ secrets.GSHEET_CLIENT_EMAIL }}
          GSHEET_PRIVATE_KEY: ${{ secrets.GSHEET_PRIVATE_KEY }}

      - id: 'update_worksheet_94'
        uses: jroehl/gsheet.action@release # you can specify '@release' to always have the latest changes
        with:
          spreadsheetId: ${{ secrets.GSHEET_ID }}
          commands: | # list of commands, specified as a valid JSON string
            [
              { "command": "getData",
                "args": {
                  "range": "Blog!A94:F94"
                }
              }
            ]
        env:
          GSHEET_CLIENT_EMAIL: ${{ secrets.GSHEET_CLIENT_EMAIL }}
          GSHEET_PRIVATE_KEY: ${{ secrets.GSHEET_PRIVATE_KEY }}

      - id: 'update_worksheet_95'
        uses: jroehl/gsheet.action@release # you can specify '@release' to always have the latest changes
        with:
          spreadsheetId: ${{ secrets.GSHEET_ID }}
          commands: | # list of commands, specified as a valid JSON string
            [
              { "command": "getData",
                "args": {
                  "range": "Blog!A95:F95"
                }
              }
            ]
        env:
          GSHEET_CLIENT_EMAIL: ${{ secrets.GSHEET_CLIENT_EMAIL }}
          GSHEET_PRIVATE_KEY: ${{ secrets.GSHEET_PRIVATE_KEY }}

      - id: 'update_worksheet_96'
        uses: jroehl/gsheet.action@release # you can specify '@release' to always have the latest changes
        with:
          spreadsheetId: ${{ secrets.GSHEET_ID }}
          commands: | # list of commands, specified as a valid JSON string
            [
              { "command": "getData",
                "args": {
                  "range": "Blog!A96:F96"
                }
              }
            ]
        env:
          GSHEET_CLIENT_EMAIL: ${{ secrets.GSHEET_CLIENT_EMAIL }}
          GSHEET_PRIVATE_KEY: ${{ secrets.GSHEET_PRIVATE_KEY }}

      - id: 'update_worksheet_97'
        uses: jroehl/gsheet.action@release # you can specify '@release' to always have the latest changes
        with:
          spreadsheetId: ${{ secrets.GSHEET_ID }}
          commands: | # list of commands, specified as a valid JSON string
            [
              { "command": "getData",
                "args": {
                  "range": "Blog!A97:F97"
                }
              }
            ]
        env:
          GSHEET_CLIENT_EMAIL: ${{ secrets.GSHEET_CLIENT_EMAIL }}
          GSHEET_PRIVATE_KEY: ${{ secrets.GSHEET_PRIVATE_KEY }}

      - id: 'update_worksheet_98'
        uses: jroehl/gsheet.action@release # you can specify '@release' to always have the latest changes
        with:
          spreadsheetId: ${{ secrets.GSHEET_ID }}
          commands: | # list of commands, specified as a valid JSON string
            [
              { "command": "getData",
                "args": {
                  "range": "Blog!A98:F98"
                }
              }
            ]
        env:
          GSHEET_CLIENT_EMAIL: ${{ secrets.GSHEET_CLIENT_EMAIL }}
          GSHEET_PRIVATE_KEY: ${{ secrets.GSHEET_PRIVATE_KEY }}

      - id: 'update_worksheet_99'
        uses: jroehl/gsheet.action@release # you can specify '@release' to always have the latest changes
        with:
          spreadsheetId: ${{ secrets.GSHEET_ID }}
          commands: | # list of commands, specified as a valid JSON string
            [
              { "command": "getData",
                "args": {
                  "range": "Blog!A99:F99"
                }
              }
            ]
        env:
          GSHEET_CLIENT_EMAIL: ${{ secrets.GSHEET_CLIENT_EMAIL }}
          GSHEET_PRIVATE_KEY: ${{ secrets.GSHEET_PRIVATE_KEY }}

      - name: dump results
        env:
          RESULTS1: ${{ steps.update_worksheet_1.outputs.results }}
          RESULTS2: ${{ steps.update_worksheet_2.outputs.results }}
          RESULTS3: ${{ steps.update_worksheet_3.outputs.results }}
          RESULTS4: ${{ steps.update_worksheet_4.outputs.results }}
          RESULTS5: ${{ steps.update_worksheet_5.outputs.results }}
          RESULTS6: ${{ steps.update_worksheet_6.outputs.results }}
          RESULTS7: ${{ steps.update_worksheet_7.outputs.results }}
          RESULTS8: ${{ steps.update_worksheet_8.outputs.results }}
          RESULTS9: ${{ steps.update_worksheet_9.outputs.results }}
          RESULTS10: ${{ steps.update_worksheet_10.outputs.results }}
          RESULTS11: ${{ steps.update_worksheet_11.outputs.results }}
          RESULTS12: ${{ steps.update_worksheet_12.outputs.results }}
          RESULTS13: ${{ steps.update_worksheet_13.outputs.results }}
          RESULTS14: ${{ steps.update_worksheet_14.outputs.results }}
          RESULTS15: ${{ steps.update_worksheet_15.outputs.results }}
          RESULTS16: ${{ steps.update_worksheet_16.outputs.results }}
          RESULTS17: ${{ steps.update_worksheet_17.outputs.results }}
          RESULTS18: ${{ steps.update_worksheet_18.outputs.results }}
          RESULTS19: ${{ steps.update_worksheet_19.outputs.results }}
          RESULTS20: ${{ steps.update_worksheet_20.outputs.results }}
          RESULTS21: ${{ steps.update_worksheet_21.outputs.results }}
          RESULTS22: ${{ steps.update_worksheet_22.outputs.results }}
          RESULTS23: ${{ steps.update_worksheet_23.outputs.results }}
          RESULTS24: ${{ steps.update_worksheet_24.outputs.results }}
          RESULTS25: ${{ steps.update_worksheet_25.outputs.results }}
          RESULTS26: ${{ steps.update_worksheet_26.outputs.results }}
          RESULTS27: ${{ steps.update_worksheet_27.outputs.results }}
          RESULTS28: ${{ steps.update_worksheet_28.outputs.results }}
          RESULTS29: ${{ steps.update_worksheet_29.outputs.results }}
          RESULTS30: ${{ steps.update_worksheet_30.outputs.results }}
          RESULTS31: ${{ steps.update_worksheet_31.outputs.results }}
          RESULTS32: ${{ steps.update_worksheet_32.outputs.results }}
          RESULTS33: ${{ steps.update_worksheet_33.outputs.results }}
          RESULTS34: ${{ steps.update_worksheet_34.outputs.results }}
          RESULTS35: ${{ steps.update_worksheet_35.outputs.results }}
          RESULTS36: ${{ steps.update_worksheet_36.outputs.results }}
          RESULTS37: ${{ steps.update_worksheet_37.outputs.results }}
          RESULTS38: ${{ steps.update_worksheet_38.outputs.results }}
          RESULTS39: ${{ steps.update_worksheet_39.outputs.results }}
          RESULTS40: ${{ steps.update_worksheet_40.outputs.results }}
          RESULTS41: ${{ steps.update_worksheet_41.outputs.results }}
          RESULTS42: ${{ steps.update_worksheet_42.outputs.results }}
          RESULTS43: ${{ steps.update_worksheet_43.outputs.results }}
          RESULTS44: ${{ steps.update_worksheet_44.outputs.results }}
          RESULTS45: ${{ steps.update_worksheet_45.outputs.results }}
          RESULTS46: ${{ steps.update_worksheet_46.outputs.results }}
          RESULTS47: ${{ steps.update_worksheet_47.outputs.results }}
          RESULTS48: ${{ steps.update_worksheet_48.outputs.results }}
          RESULTS49: ${{ steps.update_worksheet_49.outputs.results }}
          RESULTS50: ${{ steps.update_worksheet_50.outputs.results }}
          RESULTS51: ${{ steps.update_worksheet_51.outputs.results }}
          RESULTS52: ${{ steps.update_worksheet_52.outputs.results }}
          RESULTS53: ${{ steps.update_worksheet_53.outputs.results }}
          RESULTS54: ${{ steps.update_worksheet_54.outputs.results }}
          RESULTS55: ${{ steps.update_worksheet_55.outputs.results }}
          RESULTS56: ${{ steps.update_worksheet_56.outputs.results }}
          RESULTS57: ${{ steps.update_worksheet_57.outputs.results }}
          RESULTS58: ${{ steps.update_worksheet_58.outputs.results }}
          RESULTS59: ${{ steps.update_worksheet_59.outputs.results }}
          RESULTS60: ${{ steps.update_worksheet_60.outputs.results }}
          RESULTS61: ${{ steps.update_worksheet_61.outputs.results }}
          RESULTS62: ${{ steps.update_worksheet_62.outputs.results }}
          RESULTS63: ${{ steps.update_worksheet_63.outputs.results }}
          RESULTS64: ${{ steps.update_worksheet_64.outputs.results }}
          RESULTS65: ${{ steps.update_worksheet_65.outputs.results }}
          RESULTS66: ${{ steps.update_worksheet_66.outputs.results }}
          RESULTS67: ${{ steps.update_worksheet_67.outputs.results }}
          RESULTS68: ${{ steps.update_worksheet_68.outputs.results }}
          RESULTS69: ${{ steps.update_worksheet_69.outputs.results }}
          RESULTS70: ${{ steps.update_worksheet_70.outputs.results }}
          RESULTS71: ${{ steps.update_worksheet_71.outputs.results }}
          RESULTS72: ${{ steps.update_worksheet_72.outputs.results }}
          RESULTS73: ${{ steps.update_worksheet_73.outputs.results }}
          RESULTS74: ${{ steps.update_worksheet_74.outputs.results }}
          RESULTS75: ${{ steps.update_worksheet_75.outputs.results }}
          RESULTS76: ${{ steps.update_worksheet_76.outputs.results }}
          RESULTS77: ${{ steps.update_worksheet_77.outputs.results }}
          RESULTS78: ${{ steps.update_worksheet_78.outputs.results }}
          RESULTS79: ${{ steps.update_worksheet_79.outputs.results }}
          RESULTS80: ${{ steps.update_worksheet_80.outputs.results }}
          RESULTS81: ${{ steps.update_worksheet_81.outputs.results }}
          RESULTS82: ${{ steps.update_worksheet_82.outputs.results }}
          RESULTS83: ${{ steps.update_worksheet_83.outputs.results }}
          RESULTS84: ${{ steps.update_worksheet_84.outputs.results }}
          RESULTS85: ${{ steps.update_worksheet_85.outputs.results }}
          RESULTS86: ${{ steps.update_worksheet_86.outputs.results }}
          RESULTS87: ${{ steps.update_worksheet_87.outputs.results }}
          RESULTS88: ${{ steps.update_worksheet_88.outputs.results }}
          RESULTS89: ${{ steps.update_worksheet_89.outputs.results }}
          RESULTS90: ${{ steps.update_worksheet_90.outputs.results }}
          RESULTS91: ${{ steps.update_worksheet_91.outputs.results }}
          RESULTS92: ${{ steps.update_worksheet_92.outputs.results }}
          RESULTS93: ${{ steps.update_worksheet_93.outputs.results }}
          RESULTS94: ${{ steps.update_worksheet_94.outputs.results }}
          RESULTS95: ${{ steps.update_worksheet_95.outputs.results }}
          RESULTS96: ${{ steps.update_worksheet_96.outputs.results }}
          RESULTS97: ${{ steps.update_worksheet_97.outputs.results }}
          RESULTS98: ${{ steps.update_worksheet_98.outputs.results }}
          RESULTS99: ${{ steps.update_worksheet_99.outputs.results }}
        run: |
          echo "$RESULTS1" | jq ".results[0].result.rawData" > blogV2.json
          echo "," >> blogV2.json
          echo "$RESULTS2" | jq ".results[0].result.rawData" >> blogV2.json
          echo "," >> blogV2.json
          echo "$RESULTS3" | jq ".results[0].result.rawData" >> blogV2.json
          echo "," >> blogV2.json
          echo "$RESULTS4" | jq ".results[0].result.rawData" >> blogV2.json
          echo "," >> blogV2.json
          echo "$RESULTS5" | jq ".results[0].result.rawData" >> blogV2.json
          echo "," >> blogV2.json
          echo "$RESULTS6" | jq ".results[0].result.rawData" >> blogV2.json
          echo "," >> blogV2.json
          echo "$RESULTS7" | jq ".results[0].result.rawData" >> blogV2.json
          echo "," >> blogV2.json
          echo "$RESULTS8" | jq ".results[0].result.rawData" >> blogV2.json
          echo "," >> blogV2.json
          echo "$RESULTS9" | jq ".results[0].result.rawData" >> blogV2.json
          echo "," >> blogV2.json
          echo "$RESULTS10" | jq ".results[0].result.rawData" >> blogV2.json
          echo "," >> blogV2.json
          echo "$RESULTS11" | jq ".results[0].result.rawData" >> blogV2.json
          echo "," >> blogV2.json
          echo "$RESULTS12" | jq ".results[0].result.rawData" >> blogV2.json
          echo "," >> blogV2.json
          echo "$RESULTS13" | jq ".results[0].result.rawData" >> blogV2.json
          echo "," >> blogV2.json
          echo "$RESULTS14" | jq ".results[0].result.rawData" >> blogV2.json
          echo "," >> blogV2.json
          echo "$RESULTS15" | jq ".results[0].result.rawData" >> blogV2.json
          echo "," >> blogV2.json
          echo "$RESULTS16" | jq ".results[0].result.rawData" >> blogV2.json
          echo "," >> blogV2.json
          echo "$RESULTS17" | jq ".results[0].result.rawData" >> blogV2.json
          echo "," >> blogV2.json
          echo "$RESULTS18" | jq ".results[0].result.rawData" >> blogV2.json
          echo "," >> blogV2.json
          echo "$RESULTS19" | jq ".results[0].result.rawData" >> blogV2.json
          echo "," >> blogV2.json
          echo "$RESULTS20" | jq ".results[0].result.rawData" >> blogV2.json
          echo "," >> blogV2.json
          echo "$RESULTS21" | jq ".results[0].result.rawData" >> blogV2.json
          echo "," >> blogV2.json
          echo "$RESULTS22" | jq ".results[0].result.rawData" >> blogV2.json
          echo "," >> blogV2.json
          echo "$RESULTS23" | jq ".results[0].result.rawData" >> blogV2.json
          echo "," >> blogV2.json
          echo "$RESULTS24" | jq ".results[0].result.rawData" >> blogV2.json
          echo "," >> blogV2.json
          echo "$RESULTS25" | jq ".results[0].result.rawData" >> blogV2.json
          echo "," >> blogV2.json
          echo "$RESULTS26" | jq ".results[0].result.rawData" >> blogV2.json
          echo "," >> blogV2.json
          echo "$RESULTS27" | jq ".results[0].result.rawData" >> blogV2.json
          echo "," >> blogV2.json
          echo "$RESULTS28" | jq ".results[0].result.rawData" >> blogV2.json
          echo "," >> blogV2.json
          echo "$RESULTS29" | jq ".results[0].result.rawData" >> blogV2.json
          echo "," >> blogV2.json
          echo "$RESULTS30" | jq ".results[0].result.rawData" >> blogV2.json
          echo "," >> blogV2.json
          echo "$RESULTS31" | jq ".results[0].result.rawData" >> blogV2.json
          echo "," >> blogV2.json
          echo "$RESULTS32" | jq ".results[0].result.rawData" >> blogV2.json
          echo "," >> blogV2.json
          echo "$RESULTS33" | jq ".results[0].result.rawData" >> blogV2.json
          echo "," >> blogV2.json
          echo "$RESULTS34" | jq ".results[0].result.rawData" >> blogV2.json
          echo "," >> blogV2.json
          echo "$RESULTS35" | jq ".results[0].result.rawData" >> blogV2.json
          echo "," >> blogV2.json
          echo "$RESULTS36" | jq ".results[0].result.rawData" >> blogV2.json
          echo "," >> blogV2.json
          echo "$RESULTS37" | jq ".results[0].result.rawData" >> blogV2.json
          echo "," >> blogV2.json
          echo "$RESULTS38" | jq ".results[0].result.rawData" >> blogV2.json
          echo "," >> blogV2.json
          echo "$RESULTS39" | jq ".results[0].result.rawData" >> blogV2.json
          echo "," >> blogV2.json
          echo "$RESULTS40" | jq ".results[0].result.rawData" >> blogV2.json
          echo "," >> blogV2.json
          echo "$RESULTS41" | jq ".results[0].result.rawData" >> blogV2.json
          echo "," >> blogV2.json
          echo "$RESULTS42" | jq ".results[0].result.rawData" >> blogV2.json
          echo "," >> blogV2.json
          echo "$RESULTS43" | jq ".results[0].result.rawData" >> blogV2.json
          echo "," >> blogV2.json
          echo "$RESULTS44" | jq ".results[0].result.rawData" >> blogV2.json
          echo "," >> blogV2.json
          echo "$RESULTS45" | jq ".results[0].result.rawData" >> blogV2.json
          echo "," >> blogV2.json
          echo "$RESULTS46" | jq ".results[0].result.rawData" >> blogV2.json
          echo "," >> blogV2.json
          echo "$RESULTS47" | jq ".results[0].result.rawData" >> blogV2.json
          echo "," >> blogV2.json
          echo "$RESULTS48" | jq ".results[0].result.rawData" >> blogV2.json
          echo "," >> blogV2.json
          echo "$RESULTS49" | jq ".results[0].result.rawData" >> blogV2.json
          echo "," >> blogV2.json
          echo "$RESULTS50" | jq ".results[0].result.rawData" >> blogV2.json
          echo "," >> blogV2.json
          echo "$RESULTS51" | jq ".results[0].result.rawData" >> blogV2.json
          echo "," >> blogV2.json
          echo "$RESULTS52" | jq ".results[0].result.rawData" >> blogV2.json
          echo "," >> blogV2.json
          echo "$RESULTS53" | jq ".results[0].result.rawData" >> blogV2.json
          echo "," >> blogV2.json
          echo "$RESULTS54" | jq ".results[0].result.rawData" >> blogV2.json
          echo "," >> blogV2.json
          echo "$RESULTS55" | jq ".results[0].result.rawData" >> blogV2.json
          echo "," >> blogV2.json
          echo "$RESULTS56" | jq ".results[0].result.rawData" >> blogV2.json
          echo "," >> blogV2.json
          echo "$RESULTS57" | jq ".results[0].result.rawData" >> blogV2.json
          echo "," >> blogV2.json
          echo "$RESULTS58" | jq ".results[0].result.rawData" >> blogV2.json
          echo "," >> blogV2.json
          echo "$RESULTS59" | jq ".results[0].result.rawData" >> blogV2.json
          echo "," >> blogV2.json
          echo "$RESULTS60" | jq ".results[0].result.rawData" >> blogV2.json
          echo "," >> blogV2.json
          echo "$RESULTS61" | jq ".results[0].result.rawData" >> blogV2.json
          echo "," >> blogV2.json
          echo "$RESULTS62" | jq ".results[0].result.rawData" >> blogV2.json
          echo "," >> blogV2.json
          echo "$RESULTS63" | jq ".results[0].result.rawData" >> blogV2.json
          echo "," >> blogV2.json
          echo "$RESULTS64" | jq ".results[0].result.rawData" >> blogV2.json
          echo "," >> blogV2.json
          echo "$RESULTS65" | jq ".results[0].result.rawData" >> blogV2.json
          echo "," >> blogV2.json
          echo "$RESULTS66" | jq ".results[0].result.rawData" >> blogV2.json
          echo "," >> blogV2.json
          echo "$RESULTS67" | jq ".results[0].result.rawData" >> blogV2.json
          echo "," >> blogV2.json
          echo "$RESULTS68" | jq ".results[0].result.rawData" >> blogV2.json
          echo "," >> blogV2.json
          echo "$RESULTS69" | jq ".results[0].result.rawData" >> blogV2.json
          echo "," >> blogV2.json
          echo "$RESULTS70" | jq ".results[0].result.rawData" >> blogV2.json
          echo "," >> blogV2.json
          echo "$RESULTS71" | jq ".results[0].result.rawData" >> blogV2.json
          echo "," >> blogV2.json
          echo "$RESULTS72" | jq ".results[0].result.rawData" >> blogV2.json
          echo "," >> blogV2.json
          echo "$RESULTS73" | jq ".results[0].result.rawData" >> blogV2.json
          echo "," >> blogV2.json
          echo "$RESULTS74" | jq ".results[0].result.rawData" >> blogV2.json
          echo "," >> blogV2.json
          echo "$RESULTS75" | jq ".results[0].result.rawData" >> blogV2.json
          echo "," >> blogV2.json
          echo "$RESULTS76" | jq ".results[0].result.rawData" >> blogV2.json
          echo "," >> blogV2.json
          echo "$RESULTS77" | jq ".results[0].result.rawData" >> blogV2.json
          echo "," >> blogV2.json
          echo "$RESULTS78" | jq ".results[0].result.rawData" >> blogV2.json
          echo "," >> blogV2.json
          echo "$RESULTS79" | jq ".results[0].result.rawData" >> blogV2.json
          echo "," >> blogV2.json
          echo "$RESULTS80" | jq ".results[0].result.rawData" >> blogV2.json
          echo "," >> blogV2.json
          echo "$RESULTS81" | jq ".results[0].result.rawData" >> blogV2.json
          echo "," >> blogV2.json
          echo "$RESULTS82" | jq ".results[0].result.rawData" >> blogV2.json
          echo "," >> blogV2.json
          echo "$RESULTS83" | jq ".results[0].result.rawData" >> blogV2.json
          echo "," >> blogV2.json
          echo "$RESULTS84" | jq ".results[0].result.rawData" >> blogV2.json
          echo "," >> blogV2.json
          echo "$RESULTS85" | jq ".results[0].result.rawData" >> blogV2.json
          echo "," >> blogV2.json
          echo "$RESULTS86" | jq ".results[0].result.rawData" >> blogV2.json
          echo "," >> blogV2.json
          echo "$RESULTS87" | jq ".results[0].result.rawData" >> blogV2.json
          echo "," >> blogV2.json
          echo "$RESULTS88" | jq ".results[0].result.rawData" >> blogV2.json
          echo "," >> blogV2.json
          echo "$RESULTS89" | jq ".results[0].result.rawData" >> blogV2.json
          echo "," >> blogV2.json
          echo "$RESULTS90" | jq ".results[0].result.rawData" >> blogV2.json
          echo "," >> blogV2.json
          echo "$RESULTS91" | jq ".results[0].result.rawData" >> blogV2.json
          echo "," >> blogV2.json
          echo "$RESULTS92" | jq ".results[0].result.rawData" >> blogV2.json
          echo "," >> blogV2.json
          echo "$RESULTS93" | jq ".results[0].result.rawData" >> blogV2.json
          echo "," >> blogV2.json
          echo "$RESULTS94" | jq ".results[0].result.rawData" >> blogV2.json
          echo "," >> blogV2.json
          echo "$RESULTS95" | jq ".results[0].result.rawData" >> blogV2.json
          echo "," >> blogV2.json
          echo "$RESULTS96" | jq ".results[0].result.rawData" >> blogV2.json
          echo "," >> blogV2.json
          echo "$RESULTS97" | jq ".results[0].result.rawData" >> blogV2.json
          echo "," >> blogV2.json
          echo "$RESULTS98" | jq ".results[0].result.rawData" >> blogV2.json
          echo "," >> blogV2.json
          echo "$RESULTS99" | jq ".results[0].result.rawData" >> blogV2.json
          echo "," >> blogV2.json
      - uses: EndBug/add-and-commit@v7 # You can change this to use a specific version.
        with:

          # The name of the user that will be displayed as the author of the commit.
          # Default: depends on the default_author input
          author_name: Github Actions

          # The email of the user that will be displayed as the author of the commit.
          # Default: depends on the default_author input
          author_email: actions@github.com

          # The name of the branch to use, if different from the one that triggered the workflow.
          # Default: the branch that triggered the run
          branch: gh-pages

          # The message for the commit.
          # Default: 'Commit from GitHub Actions (name of the workflow)'
          message: 'Update blog.json'
